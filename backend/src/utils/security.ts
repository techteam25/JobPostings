/**
 * @deprecated Import from `@shared/utils/security` instead.
 * This re-export exists only for backward compatibility during the modular monolith migration.
 */
export { SecurityUtils } from "@shared/utils/security";
